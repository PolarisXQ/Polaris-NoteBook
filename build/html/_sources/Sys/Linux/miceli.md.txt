# Miscellaneous

<!-- 在Linux中，source和export都是用于设置环境变量的命令。

source命令用于在当前shell环境中执行指定脚本文件，并将其中的命令和变量加载到当前shell环境中。例如，使用source script.sh命令可以执行script.sh脚本文件，并将其中定义的变量和函数加载到当前shell环境中。

export命令用于将一个变量设置为环境变量，使其在当前shell环境中以及所有子进程中都可用。例如，使用export MY_VAR=value命令可以将MY_VAR变量设置为环境变量，并将其值设置为value。这样，在当前shell环境中以及所有由该shell启动的子进程中，都可以使用$MY_VAR来访问该变量的值。

sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys F42ED6FBAB17C654 -->